[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyraview"
version = "0.1.0"
description = "Pyraview Wrapper"
authors = [{name = "Jules"}]
dependencies = ["numpy"]
requires-python = ">=3.7"
